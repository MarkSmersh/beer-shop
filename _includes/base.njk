---
title: Sklep <Piwo>
---

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{ title }}</title>
        <link
            rel="icon"
            href="/assets/ico.png"
            type="image/png"
            sizes="16x16"
        />

        <style>
            @import url("https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&display=swap");

            :root {
                --primary: #111111;
                --secondary: #ffffd4;
                --primary-text: #fff;
                --secondary-text: #111;
                --accent: #fad800;
                --br: 8px;

                .dark {
                    --primary: #fad800;
                    --secondary: #111111;
                    --primary-text: #111;
                    --secondary-text: #fff;
                    --accent: #fad800;
                }
            }

            * {
                margin: 0;
                padding: 0;
                border: 0;
            }

            body {
                color: var(--secondary);
                font-family: "Lato", serif;
            }

            header {
                background-color: var(--secondary);
                display: flex;
                align-items: center;
                flex-direction: column;
                gap: 16px;
                height: 170px;

                img {
                    height: 100px;
                }

                .list {
                    list-style: none;
                    display: flex;
                    list-style: none;
                    gap: 16px;
                    height: 50px;

                    li {
                        background-color: var(--primary);
                        border-radius: var(--br);
                        padding: 8px 16px;
                        display: flex;
                        align-items: center;
                        height: fit-content;
                        transition: 0.2s;
                        border: 1px solid var(--primary);
                        font-weight: 600;
                    }

                    li > a {
                        color: var(--primary-text);
                    }

                    li:hover {
                        background-color: var(--secondary);
                    }

                    li:hover > a {
                        color: var(--secondary-text);
                    }
                }

                a {
                    text-wrap: nowrap;
                    text-decoration: none;
                }
            }

            footer {
                background-color: var(--primary);
                padding: 50px 100px;
                display: grid;
                grid: auto / 1fr 1fr;
                gap: 32px;

                .left {
                    display: flex;
                    gap: 16px;
                    flex-direction: column;
                }
            }

            main {
                min-height: calc(100vh - 170px);
            }
        </style>
    </head>
    <body>
        <header>
            <a href="/"><img src="/assets/logo.png" alt="logo" /></a>
            <div class="navbar">
                <nav>
                    <ul class="list">
                        <li>
                            <a href="">Strona głowna</a>
                        </li>
                        <li>
                            <a href="/pages/catalog">Katalog</a>
                        </li>
                        <li>
                            <a href="/pages/newsletter">Newsletter</a>
                        </li>
                        <li>
                            <a href="/pages/contact">Kontakt</a>
                        </li>
                    </ul>
                </nav>
            </div>
        </header>
        {{ content | safe }}
        <footer>
            <div class="left">
                <p>
                    Dziękujemy za odwiedzenie naszego sklepu &lt;Piwo&gt;! Nasza
                    pasja do wyjątkowych smaków oraz lokalnych browarów jest
                    widoczna w każdym oferowanym produkcie. Dbamy o najwyższą
                    jakość i zadowolenie naszych klientów.
                </p>
                <i>
                    <p class="legal">
                        <span id="year"></span>. Sklep &lt;Piwo&gt;. Wszelkie prawa zastrzeżone.<br />Zakaz
                        sprzedaży alkoholu osobom poniżej 18 roku życia.
                    </p>
                </i>
            </div>
            <div class="right">
                <p>Podpisz się na naszą rozsylkę stosownie nowości z piwem.</p>
                <div class="newsletter">
                    <form action="">
                        <input name="email" />

                    </form>
                </div>
            </div>
        </footer>
    </body>

    <script>
        // Niektóre skrypty są wywolane po lodowaniu strony

        document.addEventListener("DOMContentLoaded", (e) => {
            document.getElementById("year").textContent = new Date().getFullYear()
        })
    </script>
</html>
